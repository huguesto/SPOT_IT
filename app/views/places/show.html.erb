<div class="container">
  <div class="content">
  <div class="return-to-map">
    <a href="#" class="place-show-toggle">
      <i class="fa fa-chevron-down fa-lg" aria-hidden="true"></i>
    </a>
  </div>
    <div class="row cards-photos">
      <% if @place.spots.length == 0 %>
        <div class="col-xs-12 col-sm-6 col-md-4 text-center" >
          <p><%= "No spots yet for this place" %></p>
        </div>
      <% else %>
        <% @place.spots.each do |spot| %>
          <div class="col-xs-4 col-sm-4 col-md-4 text-center sliding" >
            <div class="polaroid mislider">
                <div class="photo" data-lightbox-gallery="photo_gallery" style="background-image: url('<%= cl_image_path spot.photo.path, width: 120, height: 100, :effect=>"art:daguerre", crop: :fill if spot.photo %>');">
                </div>
                <p><%= spot.comment %></p>
            </div>
          </div>
       <% end %>
      <% end %>
    </div>
      <div class="infos">
        <h3><%= @place.name %></h3>
        <h4><%= @place.address %></h4>
      </div>
    <div class="map-static" style="width: 100%; height: 150px; background-image: url(<%="https://maps.googleapis.com/maps/api/staticmap?center=#{@place.latitude},#{@place.longitude}&zoom=16&size=400x200&markers=color:blue%7Clabel:%7C#{@place.latitude},#{@place.longitude}&key=#{ENV['GOOGLE_API_SERVER_KEY']}"%>);">
    </div>
  </div>
</div>



<!-- #Pour chaque spot qui ont cette place_id
#display la photo prise par l'user qui a crée ce spot
#display le commentaire de l'user qui a crée ce spot
#display le created_at
( #display info de l'user avec facebook connect ) -->
