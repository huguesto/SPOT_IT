Select a Facebook's friend🤗  and discover his go-to spots.
<% spots.each do |spot| %>
  <div class="friends">
    <div class="message">
      <%= image_tag spot.user.facebook_picture_url %>
      <div class="message-name">
        <h3><%= spot.user.first_name %></h3>
      </div>
      <div class="message-body">
        <p><%= spot.inspect %></p>
      </div>
    </div>
  </div>
<%end%>
